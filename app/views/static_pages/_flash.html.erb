<% flash.each do |type, msg| %>
  <div id="alert_notice" class="alert alert-<%= type %> alert-dismissible fade show xmt-68" role="alert">
    <%= msg %> 
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>